---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: accounting-storage-service-deployment
  labels:
    app: accounting-storage-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accounting-storage-service
  template:
    metadata:
      labels:
        app: accounting-storage-service
    spec:
      containers:
      - name: accounting-storage-service
        image: registry.cloud.sdu.dk/sdu-cloud/accounting-storage-service:0.1.0
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: accounting-storage-refresh-token
      - name: token-validation
        configMap:
          name: token-validation

      imagePullSecrets:
        - name: esci-docker

---
apiVersion: v1
kind: Service
metadata:
  name: accounting-storage
spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: accounting-storage-service
