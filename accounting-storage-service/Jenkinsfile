def initialize() {

  try {
    stage('build accounting-storage-service') {
      sh '''cd accounting-storage-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'accounting-storage-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'accounting-storage-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test accounting-storage-service') {
      sh '''cd accounting-storage-service
      ./gradlew test'''
    } 
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
