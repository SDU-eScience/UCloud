---
apiVersion: batch/v1
kind: Job

metadata:
  name: project-auth-migration

spec:
  template:
    metadata:
      name: project-auth-migration

    spec:
      containers:
        - name: project-auth-service
          image: registry.cloud.sdu.dk/sdu-cloud/project-auth-service:0.1.3
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: project-auth-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: project-auth-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: project-auth-psql
        secret:
          optional: false
          secretName: project-auth-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
