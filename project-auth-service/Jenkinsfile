def initialize() {

  try {
    stage('build project-auth-service') {
      sh '''cd project-auth-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'project-auth-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'project-auth-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test project-auth-service') {
      sh '''cd project-auth-service
      ./gradlew test'''
    } 
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
