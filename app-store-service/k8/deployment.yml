---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-store
  labels:
    app: app-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-store
  template:
    metadata:
      labels:
        app: app-store
    spec:
      containers:
      - name: app-store
        image: registry.cloud.sdu.dk/sdu-cloud/app-store-service:0.3.3
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        - --config-dir
        - /etc/psql
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation
        - mountPath: /etc/psql
          name: app-store-psql
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: app-store-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
      - name: app-store-psql
        secret:
          optional: false
          secretName: app-store-psql

      imagePullSecrets:
        - name: esci-docker
