def initialize() {
  try {
    stage('build rpc-test-service') {
      sh '''cd rpc-test-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'rpc-test-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'rpc-test-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test rpc-test-service') {
      sh '''cd rpc-test-service
      ./gradlew test'''
    }
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
