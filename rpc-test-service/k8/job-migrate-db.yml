---
apiVersion: batch/v1
kind: Job

metadata:
  name: rpc-test-migration

spec:
  template:
    metadata:
      name: rpc-test-migration

    spec:
      containers:
        - name: rpc-test-service
          image: registry.cloud.sdu.dk/sdu-cloud/rpc-test-service:0.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: rpc-test-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: rpc-test-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: rpc-test-psql
        secret:
          optional: false
          secretName: rpc-test-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
