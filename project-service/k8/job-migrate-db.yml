---
apiVersion: batch/v1
kind: Job

metadata:
  name: project-migration

spec:
  template:
    metadata:
      name: project-migration

    spec:
      containers:
        - name: project-service
          image: registry.cloud.sdu.dk/sdu-cloud/project-service:1.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: project-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: project-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: project-psql
        secret:
          optional: false
          secretName: project-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
