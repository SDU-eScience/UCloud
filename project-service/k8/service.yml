---
apiVersion: v1
kind: Service
metadata:
  name: project
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v0
      kind: Mapping
      name: project
      prefix: ^/api/projects(/.*)?$
      prefix_regex: true
      service: project:8080
      rewrite: ""

spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: project