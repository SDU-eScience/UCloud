---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: indexing-service-deployment
  labels:
    app: indexing-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indexing-service
  template:
    metadata:
      labels:
        app: indexing-service
    spec:
      containers:
      - name: indexing-service
        image: registry.cloud.sdu.dk/sdu-cloud/indexing-service:1.14.5
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation

      volumes:
      - name: refresh-token
        secret:
          defaultMode: 420
          optional: false
          secretName: indexing-refresh-token
      - name: token-validation
        configMap:
          defaultMode: 420
          name: token-validation

      imagePullSecrets:
        - name: esci-docker
