---
apiVersion: batch/v1
kind: Job

metadata:
  name: app-orchestrator-migration

spec:
  template:
    metadata:
      name: app-orchestrator-migration

    spec:
      containers:
        - name: app-orchestrator-service
          image: registry.cloud.sdu.dk/sdu-cloud/app-orchestrator-service:0.3.6
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: app-orchestrator-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: app-orchestrator-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: app-orchestrator-psql
        secret:
          optional: false
          secretName: app-orchestrator-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
