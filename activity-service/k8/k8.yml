---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: activity-service-deployment
  labels:
    app: activity-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activity-service
  template:
    metadata:
      labels:
        app: activity-service
    spec:
      containers:
      - name: activity-service
        image: registry.cloud.sdu.dk/sdu-cloud/activity-service:0.2.0
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/psql
        - --config-dir
        - /etc/service-config
        - --config-dir
        - /etc/refresh-token
        volumeMounts:
        - mountPath: /etc/psql
          name: psql-secrets
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/service-config
          name: service-config

      volumes:
      - name: psql-secrets
        secret:
          optional: false
          secretName: activity-psql
      - name: refresh-token
        secret:
          optional: false
          secretName: activity-refresh-token
      - name: service-config
        configMap:
          name: activity-config

      imagePullSecrets:
        - name: esci-docker
