---
apiVersion: batch/v1
kind: Job

metadata:
  name: activity-migration

spec:
  template:
    metadata:
      name: activity-migration

    spec:
      containers:
      - args:
        - /opt/service/bin/service
        - --config-dir
        - /etc/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        - --run-script
        - migrate-db
        image: registry.cloud.sdu.dk/sdu-cloud/activity-service:1.2.0-PAGINATION.11
        name: activity-migration

        volumeMounts:
        - mountPath: /etc/service
          name: vol1
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation

      imagePullSecrets:
        - name: esci-docker

      restartPolicy: Never

      volumes:
      - name: vol1
        secret:
          optional: false
          secretName: activity-psql
      - name: refresh-token
        secret:
          optional: false
          secretName: activity-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
