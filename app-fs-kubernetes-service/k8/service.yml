---
apiVersion: v1
kind: Service
metadata:
  name: app-fs-kubernetes
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind: Mapping
      name: app-fs-kubernetes
      prefix: ^/api/app/fs/kubernetes(/.*)?$
      prefix_regex: true
      service: app-fs-kubernetes:8080
      rewrite: ""

spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: app-fs-kubernetes
