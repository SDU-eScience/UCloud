---
master:
  persistence:
    storageClass: rbd
  heapSize: 1024m
  resources:
    limits:
      cpu: "4000m"
    requests:
      cpu: "1000m"

data:
  persistence:
    storageClass: rbd
    size: 50Gi
  heapSize: 16384m
  resources:
    limits:
      cpu: "8000m"
    requests:
      cpu: "1000m"

client:
  heapSize: 1024m
  resources:
    limits:
      cpu: "4000m"
    requests:
      cpu: "1000m"

