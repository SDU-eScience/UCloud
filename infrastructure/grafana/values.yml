---
persistence:
  enabled: true
  size: 100Gi
  storageClassName: rbd
  accessModes:
  - ReadWriteOnce

sidecar:
  dashboards:
    enabled: true
    label: grafana_dashboard
  datasources:
    enabled: true
    label: grafana_datasource