---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: file-favorite
  labels:
    app: file-favorite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-favorite
  template:
    metadata:
      labels:
        app: file-favorite
    spec:
      containers:
      - name: file-favorite
        image: registry.cloud.sdu.dk/sdu-cloud/file-favorite-service:0.1.0
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: file-favorite-refresh-token
      - name: token-validation
        configMap:
          name: token-validation

      imagePullSecrets:
        - name: esci-docker

---
apiVersion: v1
kind: Service
metadata:
  name: file-favorite
spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: file-favorite
