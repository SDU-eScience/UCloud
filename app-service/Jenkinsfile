def initialize() {

  stage('build app-service') {
    sh '''cd app-service
    ./gradlew clean
    ./gradlew build -x test'''
  }

  try {
    stage('test app-service') {
      sh '''cd app-service
      ./gradlew test'''
    } 
  } catch (e) {
      echo "Test FAILED"
  } finally {
    junit '**/app-service/build/test-results/**/*.xml'      
    def currentResult = currentBuild.result ?: 'SUCCESS'
    if (currentResult == 'UNSTABLE') {
      echo "Build is unstable"
    }
    if (currentResult == 'SUCCESS') {
      jacoco  execPattern: '**/**.exec',
              exclusionPattern: '**/app-service/src/test/**/*.class,**/AuthMockingKt.class,**/DatabaseSetupKt.class', 
              sourcePattern: '**/app-service/src/main/kotlin/**'   
    }
  }
}

return this