def initialize() {

  try {
    stage('build app-service') {
      sh '''cd app-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'app-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'app-service-detekt-Report', reportTitles: ''])
  }

  try {
    stage('test app-service') {
      sh '''cd app-service
      ./gradlew test'''
    } 
  } catch (e) {
    echo "Test FAILED"
    return currentBuild.result ?: 'UNSTABLE'
  }
  return currentBuild.result ?: 'SUCCESS'
}

return this