---
apiVersion: batch/v1
kind: Job

metadata:
  name: audit-ingestion-migration

spec:
  template:
    metadata:
      name: audit-ingestion-migration

    spec:
      containers:
        - name: audit-ingestion-service
          image: registry.cloud.sdu.dk/sdu-cloud/audit-ingestion-service:0.1.7
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --config-dir
          - /etc/elasticsearch-credentials
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: audit-ingestion-psql
          - mountPath: /etc/elasticsearch-credentials
            name: elasticsearch-credentials

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: audit-ingestion-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: audit-ingestion-psql
        secret:
          optional: false
          secretName: audit-ingestion-psql
      - name: elasticsearch-credentials
        secret:
          optional: false
          secretName: elasticsearch-credentials

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
