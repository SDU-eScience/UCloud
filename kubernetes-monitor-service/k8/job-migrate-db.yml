---
apiVersion: batch/v1
kind: Job

metadata:
  name: kubernetes-monitor-migration

spec:
  template:
    metadata:
      name: kubernetes-monitor-migration

    spec:
      containers:
        - name: kubernetes-monitor-service
          image: registry.cloud.sdu.dk/sdu-cloud/kubernetes-monitor-service:0.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: kubernetes-monitor-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: kubernetes-monitor-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: kubernetes-monitor-psql
        secret:
          optional: false
          secretName: kubernetes-monitor-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
