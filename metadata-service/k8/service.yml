---
apiVersion: v1
kind: Service
metadata:
  name: metadata
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v0
      kind: Mapping
      name: metadata
      prefix: ^/api/metadata(/.*)?$
      prefix_regex: true
      service: metadata:8080
      rewrite: ""

spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: metadata-service