---
apiVersion: batch/v1
kind: Job

metadata:
  name: share-migration

spec:
  template:
    metadata:
      name: share-migration

    spec:
      containers:
        - name: share-service
          image: registry.cloud.sdu.dk/sdu-cloud/share-service:1.3.15
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: share-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: share-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: share-psql
        secret:
          optional: false
          secretName: share-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
