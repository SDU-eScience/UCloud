---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: alerting
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: ""
spec:
  rules:
  - host: cloud.sdu.dk
    http:
      paths:
      - path: /api/alerting
        backend:
          serviceName: alerting
          servicePort: 8080
