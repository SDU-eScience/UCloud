---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: alerting
  labels:
    app: alerting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alerting
  template:
    metadata:
      labels:
        app: alerting
    spec:
      serviceAccountName: alerting
      containers:
      - name: alerting
        image: registry.cloud.sdu.dk/sdu-cloud/alerting-service:1.0.8
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        - --config-dir
        - /etc/alerting
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation
        - mountPath: /etc/alerting
          name: alerting
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: alerting-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
      - name: alerting
        secret:
          optional: false
          secretName: alerting-tokens

      imagePullSecrets:
        - name: esci-docker

---
apiVersion: v1
kind: Service
metadata:
  name: alerting
spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: alerting
