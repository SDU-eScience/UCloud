---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: support-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: ""
spec:
  rules:
  - host: cloud.sdu.dk
    http:
      paths:
      - path: /api/support
        backend:
          serviceName: support
          servicePort: 8080
