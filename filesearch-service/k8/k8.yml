---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: filesearch-service-deployment
  labels:
    app: filesearch-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: filesearch-service
  template:
    metadata:
      labels:
        app: filesearch-service
    spec:
      containers:
      - name: filesearch-service
        image: registry.cloud.sdu.dk/sdu-cloud/filesearch-service:0.1.0
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: filesearch-refresh-token

      imagePullSecrets:
      - name: esci-docker
