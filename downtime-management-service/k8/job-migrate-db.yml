---
apiVersion: batch/v1
kind: Job

metadata:
  name: downtime-management-migration

spec:
  template:
    metadata:
      name: downtime-management-migration

    spec:
      containers:
        - name: downtime-management-service
          image: registry.cloud.sdu.dk/sdu-cloud/downtime-management-service:0.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: downtime-management-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: downtime-management-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: downtime-management-psql
        secret:
          optional: false
          secretName: downtime-management-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
