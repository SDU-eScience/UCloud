def initialize() {
  try {
    stage('build downtime-management-service') {
      sh '''cd downtime-management-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'downtime-management-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'downtime-management-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test downtime-management-service') {
      sh '''cd downtime-management-service
      ./gradlew test'''
    }
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
