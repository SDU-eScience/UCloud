---
apiVersion: batch/v1
kind: Job

metadata:
  name: app-license-migration

spec:
  template:
    metadata:
      name: app-license-migration

    spec:
      containers:
        - name: app-license-service
          image: registry.cloud.sdu.dk/sdu-cloud/app-license-service:0.1.3
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: app-license-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: app-license-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: app-license-psql
        secret:
          optional: false
          secretName: app-license-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
