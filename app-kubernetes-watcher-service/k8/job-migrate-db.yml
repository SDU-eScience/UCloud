---
apiVersion: batch/v1
kind: Job

metadata:
  name: app-kubernetes-watcher-migration

spec:
  template:
    metadata:
      name: app-kubernetes-watcher-migration

    spec:
      containers:
        - name: app-kubernetes-watcher-service
          image: registry.cloud.sdu.dk/sdu-cloud/app-kubernetes-watcher-service:0.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: app-kubernetes-watcher-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: app-kubernetes-watcher-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: app-kubernetes-watcher-psql
        secret:
          optional: false
          secretName: app-kubernetes-watcher-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
