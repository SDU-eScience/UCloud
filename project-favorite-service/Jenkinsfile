def initialize() {
  try {
    stage('build project-favorite-service') {
      sh '''cd project-favorite-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'project-favorite-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'project-favorite-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test project-favorite-service') {
      sh '''cd project-favorite-service
      ./gradlew test'''
    }
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
