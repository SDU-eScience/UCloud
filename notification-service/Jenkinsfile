def initialize() {

  try {
    stage('build notification-service') {
      sh '''cd notification-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'notification-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'notification-service-detekt-Report', reportTitles: ''])
  }
  
  try {
    stage('test notification-service') {
      sh '''cd notification-service
      ./gradlew test'''
    } 
  } catch (e) {
    echo "Test FAILED"
    return  'UNSTABLE'
  }
  return 'SUCCESS'
}

return this