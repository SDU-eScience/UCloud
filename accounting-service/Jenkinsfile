def initialize() {

  try {
    stage('build accounting-service') {
      sh '''cd accounting-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'accounting-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'accounting-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test accounting-service') {
      sh '''cd accounting-service
      ./gradlew test'''
    } 
  } catch (e) {
    echo "Test FAILED"
    return currentBuild.result ?: 'UNSTABLE'
  }
  return currentBuild.result ?: 'SUCCESS'
}

return this
