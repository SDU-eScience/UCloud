---
apiVersion: v1
kind: Service
metadata:
  name: app-dummy
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v0
      kind: Mapping
      name: app-dummy
      prefix: ^/api/app/dummy(/.*)?$
      prefix_regex: true
      service: app-dummy:8080
      rewrite: ""

spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: app-dummy
