---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-dummy
  labels:
    app: app-dummy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-dummy
  template:
    metadata:
      labels:
        app: app-dummy
    spec:
      containers:
      - name: app-dummy
        image: registry.cloud.sdu.dk/sdu-cloud/app-dummy-service:0.1.0
        command:
        - /opt/service/bin/service
        - --config-dir
        - /etc/refresh-token
        - --config-dir
        - /etc/token-validation
        - --config-dir
        - /etc/psql
        volumeMounts:
        - mountPath: /etc/refresh-token
          name: refresh-token
        - mountPath: /etc/token-validation
          name: token-validation
        - mountPath: /etc/psql
          name: app-dummy-psql
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: app-dummy-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
      - name: app-dummy-psql
        secret:
          optional: false
          secretName: app-dummy-psql

      imagePullSecrets:
        - name: esci-docker
