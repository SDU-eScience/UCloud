---
apiVersion: batch/v1
kind: Job

metadata:
  name: webdav-migration

spec:
  template:
    metadata:
      name: webdav-migration

    spec:
      containers:
        - name: webdav-service
          image: registry.cloud.sdu.dk/sdu-cloud/webdav-service:0.1.9
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: webdav-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: webdav-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: webdav-psql
        secret:
          optional: false
          secretName: webdav-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
