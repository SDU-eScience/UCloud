---
apiVersion: batch/v1
kind: Job

metadata:
  name: ping-migration

spec:
  template:
    metadata:
      name: ping-migration

    spec:
      containers:
        - name: ping-service
          image: registry.cloud.sdu.dk/sdu-cloud/ping-service:0.1.0
          args:
          - /opt/service/bin/service
          - --config-dir
          - /etc/refresh-token
          - --config-dir
          - /etc/token-validation
          - --config-dir
          - /etc/psql
          - --run-script
          - migrate-db
          volumeMounts:
          - mountPath: /etc/refresh-token
            name: refresh-token
          - mountPath: /etc/token-validation
            name: token-validation
          - mountPath: /etc/psql
            name: ping-psql

      volumes:
      - name: refresh-token
        secret:
          optional: false
          secretName: ping-refresh-token
      - name: token-validation
        configMap:
          name: token-validation
          defaultMode: 420
      - name: ping-psql
        secret:
          optional: false
          secretName: ping-psql

      restartPolicy: Never
      imagePullSecrets:
      - name: esci-docker
