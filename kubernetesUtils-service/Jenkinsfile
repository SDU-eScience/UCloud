def initialize() {
  try {
    stage('build kubernetesUtils-service') {
      sh '''cd kubernetesUtils-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'kubernetesUtils-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'kubernetesUtils-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test kubernetesUtils-service') {
      sh '''cd kubernetesUtils-service
      ./gradlew test'''
    }
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
