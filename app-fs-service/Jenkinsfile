def initialize() {
  try {
    stage('build app-fs-service') {
      sh '''cd app-fs-service
      ./gradlew clean
      ./gradlew build -x test'''
    }
  } catch (e) {
    echo "Build Failed"
    return currentBuild.result ?: 'FAILURE'
  } finally {
    publishHTML([allowmissing: false, alwaysLinkToLastBuild: false, keepAll: true, reportDir: 'app-fs-service/build/reports/detekt', reportFiles: 'detekt.html', reportName: 'app-fs-service-detekt-Report', reportTitles: ''])
  }
  try {
    stage('test app-fs-service') {
      sh '''cd app-fs-service
      ./gradlew test'''
    }
  } catch (e) {
    echo "Test FAILED"
    return 'UNSTABLE'
  }
  return 'SUCCESS'
}

return this
