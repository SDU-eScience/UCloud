---
apiVersion: v1
kind: Service
metadata:
  name: app-fs
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind: Mapping
      name: app-fs
      prefix: ^/api/app/fs(/.*)?$
      prefix_regex: true
      service: app-fs:8080
      rewrite: ""

spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: app-fs
